# Migration 010 — 2.4.1 (temp/ rendering aid + CLAUDE.md enforcement fix)

Summary: ICEA and Tech Spec drafts now written to temp/ for VS Code preview.
CLAUDE.md write gate table and keyword handler table updated to enforce
plan-first sequence and temp/ flow — fixes Claude Code skipping the plan
phase and writing ICEA inline.

## Changes applied by /dream-sync

### 1. CLAUDE.md — Write Gate table

Replace the `## 0. WRITE GATE` artefact table with the updated version.
Check for the old `*.icea.md — Immediately after draft` row as the signal
that the table is stale:

```bash
grep -q "Immediately after draft" ./CLAUDE.md
```

If found, the table needs updating. Copy the `## 0. WRITE GATE` section
from the installed plugin's CLAUDE.md to replace the stale one. The new
table includes:
- `temp/ADO-{ID}-icea.md` and `temp/ADO-{ID}-tech.md` rows
- Correct trigger for `*.icea.md` (On SAVE ICEA, not immediately)
- Correct trigger for `*.techspec.md` (On SAVE TECH, not immediately)
- Pre-plan gate rule block

### 2. CLAUDE.md — Keyword Handlers table

Check for stale "immediately draft ICEA inline" text:

```bash
grep -q "draft ICEA inline\|draft Tech Spec inline" ./CLAUDE.md
```

If found, update the three affected rows:
- `SAVE PLAN ADO-{ID}` → write plan, write ICEA to temp/
- `SAVE ICEA ADO-{ID}` → copy temp ICEA to permanent, write Tech Spec to temp/

Copy updated rows from the installed plugin's CLAUDE.md.

### 3. .gitignore — add temp/ to managed block

Run `/gitignore-sync` to add `temp/` to the managed block.
Alternatively, add it manually inside the managed block:
```
temp/
```

### 4. No skill, stub, or hook changes
All command stubs, skills, and hooks unchanged from 2.4.0.
