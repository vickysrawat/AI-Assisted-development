# Migration to 3.0.0
Summary: Knowledge graph becomes the single orientation layer; domain-map.md retired; the graph is now committed (not gitignored).

## Sync actions (idempotent — never overwrite developer content)
- Ensure the knowledge graph exists: if `.claude/graph/graph-index.md` is missing,
  run the `architect` skill (Step 7) via `/dream-init` to generate it.
- Delete the retired orientation file `.claude/architecture/domain-map.md` if present
  (superseded by `.claude/graph/`). The other `.claude/architecture/*.md` docs stay.
- Ensure `.claude/graph/` is **not** ignored: remove any `.claude/graph/` line from the
  managed block of `.gitignore`/`.tfignore` (it was never in the canonical managed block,
  but older hand-edited ignore files may list it). The graph is committed from v3.0.0.
- No new hooks or rule files. The graph stale-detection hooks (post-merge/post-checkout)
  are unchanged from v2.6.0.

## Notes
- Breaking: any project script that reads `.claude/architecture/domain-map.md` must switch
  to `.claude/graph/graph-index.md` (+ per-module `.claude/graph/<module>.md`).
- Governing decision: ADR 0038 (supersedes ADR 0017). `domain-map-spec.md` was removed
  from `components.shared`.
- After sync, run `/dream-status` — check 1f now reports the graph index (not domain-map).
