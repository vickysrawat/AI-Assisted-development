# Migration 031 — v3.23.0

## Summary

`icea-floor` hook refresh — bug-aware block message, PowerShell-port exemption parity + ASCII
strings, and a new loud/audited `SKIP_ICEA_FLOOR` escape hatch (mirrors `SKIP_FINDINGS_GATE`).

## Sync actions (idempotent — never overwrite developer content)

- Re-copy the `icea-floor.*` hooks (`icea-floor.sh`, `icea-floor.cjs`, `icea-floor.ps1`) — picked up
  by the 1p hook-hash check. Brings the bug-aware block message, the PS1 exemption parity fix
  (`CodeReviews`/`security`/`dynamic-scan`/`token-analysis`), the PS1 ASCII-string fix, and the
  `SKIP_ICEA_FLOOR` escape hatch to the project's `.claude/hooks/`.

## Notes

- Hooks only — no state/schema/ignore-block changes.
- Default behaviour unchanged: the floor still blocks guarded source writes when no approved ICEA
  (or `Tier: T1` bug spec) exists. The escape hatch is inert unless `SKIP_ICEA_FLOOR=1` is set, and
  even then requires `ICEA_FLOOR_JUSTIFICATION` and logs a `gate.bypass` audit event.
- The escape hatch is session-wide (a PreToolUse hook reads the process env; it cannot be scoped to
  one write) — a loud warning is emitted on every bypassed write.
