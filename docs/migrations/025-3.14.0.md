# Migration 025 — v3.14.0

## Summary

Migration skill upgraded to an advisor: new Stage 0.5 "Target Options Analysis" (recommends
2–3 scored target stacks + a migration posture as an ADR before Q1, gated by
`APPROVE OPTIONS ADO-{ID}`) and new Stage 5.0 "Golden-Master behavioral verification" (records a
request/response oracle from the running source and replays it against the target). Enforcement
gaps closed (real coverage measurement, an implemented integration-contract-hash gate, honest
unsupported-source handling). Generation-tier model default refreshed
`claude-opus-4-6` → `claude-opus-4-8`.

## Change type

Skill/spec enhancement (plugin-cache side) + one CLAUDE.md §0a template addition. **No required
project-side state migration.** The migration skill and its reference specs are read live from the
plugin cache, so an upgraded install picks them up automatically — this entry exists for the
record and for the one template change below.

## Required action

None mandatory. Optionally run `/setup-sync` (or `/dream-sync`) to refresh the deployed CLAUDE.md
template so `MIGRATE OPTIONS ADO-{ID}` appears in the project's §0a keyword-handlers table. Even
without a sync, the migration skill already recognises `MIGRATE OPTIONS` from its own trigger list,
so routing works regardless.

## Sync actions (idempotent — never overwrite developer content)

1. If the project's CLAUDE.md §0a is a managed section, ensure the `MIGRATE OPTIONS ADO-{ID}` row
   is present (added between `MIGRATE STATUS` and `MIGRATE ARCH`).

## New files (in plugin — accessed via $PLUGIN_DIR)

- `skills/migration/references/specs/target-options-spec.md` — Stage 0.5 decision-support document.
- `skills/migration/references/specs/golden-master-spec.md` — Stage 5.0 behavioral-oracle spec.
- `skills/migration/references/specs/source-inventory-spec.md` — Stage 0.6 source behavioral inventory (human review-gate before rewrite).
- `skills/migration/references/strategies/README.md` — target execution-profile token contract.
- `skills/migration/references/strategies/dotnet.md` + `angular.md` — implemented execution profiles.
- `skills/migration/references/strategies/java-spring.md` — implemented (⚠ unverified) → `.NET → Java` now executable.
- `skills/migration/references/strategies/python.md` — implemented (⚠ unverified) → selectable via `nodejs → python`.
- `skills/migration/references/stacks/python.md` — Python/FastAPI stack reference (new).
- `skills/migration/references/mappings/nodejs-python.md` — Node/Express → FastAPI parity mapping (new).
- `skills/migration/references/strategies/react.md` — React (Vite) frontend execution profile (new).
- `skills/migration/references/mappings/angular-react.md` — Angular → React parity mapping (new).

## Skill / spec / config changes

- `skills/migration/SKILL.md` v1.6 → v1.16: Stage 0.5 + Stage 0.6 (source behavioral inventory
  review-gate before rewrite — first-class Gaps Report, Given/When/Then + verbatim outcomes, mandatory
  error/edge coverage, index+per-cluster output, immutable signed baseline with append-only Stage 5.0
  verification, stable feature-IDs) + Stage 5.0; full-stack = two coordinated single-track runs
  (`mode.track` backend|frontend|upgrade — a backend run publishes the API contract, a frontend run
  consumes it; Step 4.6 is the frontend run's Stage 4 with a generated API client — target-agnostic,
  resolving from `angular.md` OR `react.md`, so React is a frontend target too (angular→react));
  target guardrail rules deployed at
  Step 3.3a (before Stage 4, committed with the skeleton, read by each cluster agent); fully
  stack-agnostic Stages 3–6 — ALL .NET literals moved into `strategies/*.md` and referenced by
  `{profile TOKEN}`; profile loads at Stage 3; Q1 now confirms the recommended
  target; coverage measurement wired into Stage 5.2; contract-hash computed at Step 3.2 §7 and
  checked in new Step 4.3a; honest unsupported-source STOP; model fallbacks opus-4-6 → opus-4-8;
  checkpoint schema 1.6 → 1.8 (adds `options_approved` + `inventory_approved`); new hard rules.
- `skills/shared/model-routing-spec.md` v1.3 → v1.4: generation-tier default opus-4-6 → opus-4-8.
- `_project-deploy/CLAUDE.md` + root `CLAUDE.md` §0a: new `MIGRATE OPTIONS ADO-{ID}` keyword row;
  stack-support-is-a-subset note in the header comment.

## Checkpoint schema change

`.claude/migration-checkpoint.json` schema 1.6 → 1.8 — adds `stage_gates.options_approved` (Stage 0.5) and `stage_gates.inventory_approved` (Stage 0.6).
Runtime-only file (gitignored, per-migration); no migration of existing checkpoints needed — a
1.6 checkpoint remains resumable and the missing gate defaults acceptably.

## No hook change required
## No rule file change required
