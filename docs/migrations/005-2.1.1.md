# Migration 005 — 2.1.1

Summary: install.ps1 rewrite — dynamic version, local-folder update,
CLAUDE.md and dream-init-state.json auto-patching, marketplace.json
path fix.

## Changes applied by /dream-sync

### 1. install.ps1 — full rewrite
Re-copy from installed plugin. Key changes:
- Version read from plugin.json dynamically — never hardcoded
- -Update asks: git pull or local folder (same as fresh install)
- Post-install/update: patches Plugin version line in CLAUDE.md files
- Post-install/update: patches dream_init_plugin_version in dream-init-state.json
- marketplace.json written to correct path ($MARKETPLACE_DIR\marketplace.json)
  not the wrong .claude-plugin subdirectory
- Marketplace description version is dynamic
- Command count in next-steps panel is dynamic

### 2. No skill or CLAUDE.md changes
All skill files unchanged from 2.1.0.
