# Migration 002 — 1.31.0

Summary: Write Gate, hierarchical ICEA folder structure, Tech Spec companion doc.

## Version-sensitive changes (applied by /dream-sync)

### 1. CLAUDE.md — Section 0 (Write Gate)
Add Section 0 before Section 1. See CLAUDE.md in the installed plugin for
the exact text. This is the most critical change — without it the write gate
is not active.

### 2. CLAUDE.md — Feature Gate path
Update the `docs/icea/ADO-[ID]-*.md` reference to:
`docs/Release{RELEASE_ID}/Sprint{SPRINT_ID}/UserStory{ADO_ID}/ADO-{ADO_ID}-*.icea.md`

### 3. hooks/icea-floor.sh
Re-copy from the installed plugin. The find pattern and write-exemption case
have both changed — a partial edit risks leaving the floor inert.

### 4. Existing ICEA files
Legacy files under `docs/icea/` continue to be matched by the floor hook
(backward compat pattern retained). No migration of existing files is
required, but teams may wish to move them manually to the new structure.
