# Migration 023 — v3.12.1

## Summary
Bug fix: corrected all broken plugin path references in skill SKILL.md files, references/ subdirectory files, and command markdown files.

## Change type
Runtime-only fix. No target project files changed.

## Required action
**None.** This fix affects files loaded from the plugin cache at runtime (skills/ and commands/ directories). Target projects do not need to run setup-sync — the fix takes effect automatically on next skill invocation.
