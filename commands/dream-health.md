---
description: Generate memory/health.html — a self-contained browser dashboard showing confidence scores, decay curve, promote candidates, and dream run history with clickable justification panels.
argument-hint: (no arguments needed)
---

# /dream-health

> **Plugin path:** Read `.claude/plugin-path.txt` to get `PLUGIN_DIR`. If absent, use the Node.js resolver from `skills/shared/plugin-path-resolution.md §1a`.

Read `$PLUGIN_DIR/skills/dream-health/SKILL.md` and execute it in full.
