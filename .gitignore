.DS_Store
Thumbs.db
.vscode/
.idea/
*.local.*

# Generated output dirs — anchored to repo root with a leading slash so they do
# NOT also match the skill source dirs skills/token-analysis/ and skills/dynamic-scan/.
# (Unanchored names match at any depth — that had been ignoring those skills' source.)


# Generated by Dream skills — do not commit
.claude/file-cache.json
memory/health.html
/token-analysis/

# PAT credential storage — NEVER commit
.claude/settings.json
.claude/settings.local.json
tests/results/

# Skill checkpoint files — runtime state, do not commit
.claude/security-checkpoint.json
.claude/code-review-checkpoint.json

# Generated by dynamic-scan command — may contain plaintext credentials in session/context files
/dynamic-scan/
