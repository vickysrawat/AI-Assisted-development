---
description: "Create a Pull Request in Azure DevOps directly from Claude Code, or save a complete PR draft for manual submission. Generates the PR description, validates against ICEA, and submits via ADO API."
argument-hint: "(no arguments needed — reads branch and ICEA automatically)"
---

# /pr-create
<skill>pr-create</skill>

Invoke the pr-create skill to create a Pull Request in Azure DevOps.
