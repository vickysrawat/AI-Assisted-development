# Tech Spec — Epic-Level Template
# Audience: QA, DevOps, Reviewers, Tech Lead
# This document covers cross-cutting concerns for the entire epic.
# Per-story implementation detail lives in ADO-{ID}-Story-{N}-{name}.techspec.md
#
# Generated by: icea-feature Step 8 (EPIC branch)
# Saved by: SAVE TECH ADO-{ID}

---

# Epic Tech Spec — {Feature Name}
ADO #{ID} · Release {R} · Sprint {S}
Status: DRAFT · EPIC · {N} SP total

---

## Overview

{One paragraph: what this epic delivers end-to-end, which major components it
introduces or modifies, and the governing architectural pattern. Written for a
reviewer or QA engineer picking this up cold. Include scope boundaries — what
this epic deliberately does NOT do.}

---

## Story Breakdown

All implementation detail (AC Coverage Matrix, Files Changed, Test Cases) lives in
each story's individual tech spec linked below.

| Story | Title | SP | Shippable alone? | Depends on | Tech Spec | Status |
|---|---|---|---|---|---|---|
| 1 | {Shared Components / Foundation} | {N} | Yes | None | [ADO-{ID}-Story-1-{name}.techspec.md]() | ⏳ Pending |
| 2 | {Title} | {N} | Yes / No | Story 1 | [ADO-{ID}-Story-2-{name}.techspec.md]() | ⏳ Pending |

> Story 1 is always the shared-components story — it owns all new database schema (DDL),
> shared service contracts, and common types that later stories depend on.
> Stories are broken by logical completion — each is a shippable slice (≤5 SP).
> Never broken by AC.

---

## Auth & Security

{Cross-cutting auth and security concerns that apply to all stories in this epic.
Per-story endpoint-level auth detail lives in each story's tech spec.}

**Authentication pattern:** {e.g. Azure AD + Bearer token / Cookie auth / API key}

**Authorisation:** {role or claim required for the epic's features}

**Cross-cutting security concerns:**

| Concern | Mitigation |
|---|---|
| {e.g. XSS} | {e.g. Angular template binding, no innerHTML} |
| {e.g. SQL injection} | {e.g. Dapper parameterised queries throughout} |
| {e.g. CSRF} | {e.g. Anti-forgery token on all POST/PUT/DELETE} |

---

## Overall Request Flow

{End-to-end sequence showing how the epic's key flows work across stories.
Use prose or ASCII diagram. Include: browser → API → service → DB → response.
Show which story introduces each layer.}

```
Example:
Browser → [Story 2] Angular component
       → [Story 1] POST /api/{endpoint} (ASP.NET controller)
       → [Story 1] {ServiceName}.{Method}()
       → [Story 1] Dapper → {TableName}
```

---

## Rollback

{Epic-level rollback plan. Is the epic purely additive? Are there data migrations?}

**Schema migrations:** {Yes — migration script at {path} / No schema changes}
**Rollback procedure:**
1. {Step 1 — e.g. revert deployment to prior build via ADO pipeline}
2. {Step 2 — e.g. run down-migration if schema was applied}
3. {Step 3 — e.g. verify health checks pass}

**Per-story rollback:** Each story tech spec contains story-level rollback detail.

---

## Handover

### QA Team

**What was added:** {Summary of new functionality across all stories}

**Test entry points:**
- Story 1: {environment, test data, manual steps}
- Story 2: {environment, test data, manual steps}

**Regression risk:** {Which existing flows could be affected — e.g. {FeatureName} search,
{FeatureName} export. Verify these are unaffected after each story ships.}

**Test data:** {How to create test data — seed script, ADO test plan, manual setup}

### DevOps / Platform Team

{Azure App Config changes, Key Vault secrets, new environment variables, new HTTP
clients, Docker/AKS changes, pipeline changes, DB migrations, health check impact.
If none, state explicitly. Summarise across all stories; story-level detail in each
story's tech spec.}

| Item | Story | Detail |
|---|---|---|
| {e.g. New env var: FEATURE_FLAG_X} | Story 1 | {value, environment} |
| {e.g. DB migration} | Story 1 | {migration file path} |
| {e.g. Key Vault secret} | Story 2 | {secret name} |

### Future Developer — Follow-on Work

{Step-by-step instructions for the next developer extending this epic.
Include: where Story 1's shared components live, extension points, known gaps.}

---

## Definition of Done — Epic

The epic is done when ALL of the following are true:

**Delivery**
- [ ] All {N} story tech specs generated and saved (tracker shows all ✅)
- [ ] All stories implemented, reviewed, and merged (tracker Child ADO # filled)
- [ ] All child ADOs closed in Azure DevOps

**Quality**
- [ ] All story-level unit and integration tests pass
- [ ] Regression verified across all existing flows listed in QA Handover
- [ ] No hardcoded secrets, connection strings, or credentials in any story

**Review**
- [ ] Epic tech spec reviewed by Tech Lead and Product
- [ ] Each story's PR maps changed files to ACs (AC Coverage Matrix)
- [ ] ICEA and all story tech specs committed in the feature branch

---

## Reviewer Checklist

{Cross-cutting things the Tech Lead or code reviewer should verify across all stories:}

- [ ] Story 1 schema matches the overall data model — no missing tables or columns
- [ ] Auth & Security section: every story's endpoints enforce the stated auth pattern
- [ ] No story breaks the ≤5 SP / shippable-slice rule
- [ ] Dependencies column in Story Breakdown matches actual implementation order
- [ ] Each story's AC Coverage Matrix has no gaps (all ACs → files, all files → ACs)
- [ ] {Stack-specific cross-cutting check — e.g. all Dapper queries parameterised}
- [ ] {Stack-specific cross-cutting check — e.g. no raw HTML in Angular templates}

---

## Open Questions

| # | Question | Owner | Deadline | Status |
|---|---|---|---|---|
| ❓[1] | {question} | {owner} | {deadline} | Open |

> All open questions must be resolved before SAVE TECH. No CONFIRM bypass.

---

## Revision Log

{YYYY-MM-DD} — Epic tech spec drafted
