<!-- TEMPLATE -->
# Architecture — Decision Log

> Load this file to understand *why* the architecture is the way it is, before changing a
> long-standing pattern. **Append-only and PR-reviewed** — add a new `AD-NNN` entry when a
> non-trivial choice is made; never rewrite history.
>
> Seeded from detectable non-obvious choices; the **Rationale is human** — the architect never
> invents it (left as `⚠` until a person fills it in).

## AD-001: {decision title}

- **Decision:** {what was chosen — detectable from code/config/CLAUDE.md}
- **Rationale:** ⚠ Could not determine — needs manual input (the *why*)
- **Alternatives rejected:** {other options that were on the table}
- **Date:** {YYYY-MM-DD or "unknown"}
- **Status:** Accepted | Superseded by AD-NNN

<!-- Seed at most 3-5 entries from evidence: data-access approach, auth model, framework/
     layering choices, key libraries. Fill Decision + Alternatives; leave Rationale flagged. -->
