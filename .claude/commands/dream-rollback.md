---
description: "Reverses a specific Dream consolidation run. Lists run history, shows changes to reverse, confirms before applying. The rollback is logged and itself reversible.  Example: /dream-rollback"
argument-hint: "(no arguments — interactive) | --help"
---

If the argument is `--help` or `?help`, output the following block verbatim and stop — do not invoke the skill:

```
/dream-rollback — Reverse a Dream consolidation run.

Lists the run history, shows what will be reversed, and asks for confirmation
before applying. The rollback itself is logged and reversible.

Arguments:
  (no arguments)   Start the interactive rollback flow.
  --help, ?help    Show this help.

Example:
  /dream-rollback
```

<skill>ai-assisted-development:dream-rollback</skill>
