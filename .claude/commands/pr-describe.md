---
description: "Generate a complete, ICEA-compliant pull request description. Reads git diff automatically, maps every changed file to an Acceptance Criterion, flags scope creep, and outputs a self-review checklist. Triggers on: 'write PR', 'PR description', 'ready to raise a PR'."
argument-hint: "(no arguments needed — reads git diff automatically)"
---

# /pr-describe
<skill>pr-describe</skill>

Invoke the pr-describe skill to generate a PR description for the current branch.
