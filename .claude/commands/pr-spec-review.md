---
description: "Review a Pull Request against a functional specification or ICEA document. Produces a structured pass/fail compliance report. Use when checking a PR against a spec before merging."
argument-hint: "(no arguments needed — paste the spec when prompted)"
---

# /pr-spec-review
<skill>pr-spec-review</skill>

Invoke the pr-spec-review skill to review a PR against a spec or ICEA.
