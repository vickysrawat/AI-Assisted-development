---
description: "Generate memory/health.html — a self-contained browser dashboard showing confidence scores, decay curve, promote candidates, and dream run history with clickable justification panels.  Example: /dream-health"
argument-hint: "(no arguments) | --help"
---

# /dream-health

If the argument is `--help` or `?help`, output the following block verbatim and stop — do not invoke the skill:

```
/dream-health — Generate a memory health dashboard (memory/health.html).

Opens a self-contained HTML dashboard showing: confidence score distribution,
decay curve, entries ready for promotion to topic files, and dream run history
with clickable justification panels.

Arguments:
  (no arguments)   Generate the dashboard.
  --help, ?help    Show this help.

Example:
  /dream-health
```

<skill>ai-assisted-development:dream-health</skill>
