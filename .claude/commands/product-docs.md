---
description: "Generate a Product Detail Document and/or User Guide for the current project as self-contained HTML files. Asks which documents you want before creating anything.  Example: /product-docs"
argument-hint: "(no arguments — asks interactively) | --help"
---

If the argument is `--help` or `?help`, output the following block verbatim and stop — do not invoke the skill:

```
/product-docs — Generate product documentation as self-contained HTML files.

Generates a Product Detail Document and/or User Guide. Asks which documents
you want before creating anything — skips any you decline.

Arguments:
  (no arguments)   Start the interactive document generation flow.
  --help, ?help    Show this help.

Example:
  /product-docs
```

<skill>ai-assisted-development:product-docs</skill>
