---
description: "Run a security review. Omit the flag to be prompted with an interactive scope menu. Flags: --changed (staged files only), --pr (branch diff), --full (all files, ignore cache), --ci (full + cache warning), --area backend|frontend|config|<Module>, --continue (resume checkpoint)."
argument-hint: "[--full | --changed | --pr | --area <type> | --continue]  — omit for interactive menu"
---
<command>security-review</command>
