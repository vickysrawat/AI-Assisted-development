---
description: Targeted architecture doc refresh — updates architecture.md prose for changed areas, refreshes a single doc (--data / --integrations / --security), appends a decision (--decisions), or re-runs the deployment questionnaire (--deployment). For the module graph, use /graph-sync. Optional path argument.
---
<command>update-arch</command>
