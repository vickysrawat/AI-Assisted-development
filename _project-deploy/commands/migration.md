---
name: migration
description: >
  Migrate an application to a new tech stack. Run from INSIDE the new target project folder.
  Provide the source application path when prompted. Supports: .NET Framework → .NET 10,
  Java ↔ .NET, React+Express → Angular+.NET, Node.js → .NET.
  Usage: mkdir my-new-app && cd my-new-app && /migration
---

Invoke skill at $PLUGIN_DIR/skills/migration/SKILL.md
